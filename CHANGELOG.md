# 更新日志

本文记录 MotionWallpaper 的重要变更。

## 未发布

- 重构原生壁纸工作区，分离壁纸、屏幕、任务、设置与存储；选图先预览，再明确应用到单屏或全部屏幕，并提供屏幕编号识别与高清静态预览。
- 设置页采用左右两栏，窄窗口自动切换为单栏，调整窗口大小时控件保持完整可见。
- 新增固定数据目录的开发版启动入口，支持重新构建、运行测试并启动，无需反复安装。
- 托盘暂停/恢复与“活动时播放”共用持久化开关，设置页自动同步；普通设置保存不会覆盖托盘刚修改的状态，屏保与自动暂停保持独立。
- 性能副本已取消、停止、删除或失败时显示具体原因及静态预览状态，提供重新生成入口；重复点击同一任务不会从头重建。
- 新配置默认平衡，视频导入后自动创建所选档位的持久任务；重复导入不会取消现有任务或清除失败、取消记录。失败任务保留原因和重试入口，最终校验失败不会再次自动排队。
- 关闭活动播放后保留完整分辨率的静态画面，并尽快释放解码器和交换链；全屏覆盖等短暂停最多保留 30 秒的快速恢复窗口，屏保退出至静态桌面时尽快释放。
- 重复暂停不再延长资源驻留时间；重复冻结不会重新创建已释放的解码器，过期的暂停定时器不会打断恢复播放。

## [1.0.0] - 2026-09-23

1.0 系列首个正式版本，以已验证的 0.2.1 功能为基线。

### 播放与画质

- Windows 缺少源视频解码器时，可以回退到随包 HEVC/H.264 解码器，优先使用当前播放显卡的 D3D11 硬解；不安装全局解码器，也不改写原文件。
- 同一显卡、同一视频、同步播放的屏幕共享解码，各屏独立跟随刷新节奏，降低重复解码负担。
- 平衡副本匹配显示器物理分辨率和刷新率；节能副本最高 1080p，帧率同样跟随刷新率。两者均不放大低规格源、不插帧、不超过源帧率。
- 原画失败时提供详情和生成平衡/节能副本的恢复入口；用户发起转换后才生成并应用。
- 静态桌面预览按需从原视频提取最高 4K 的画面，列表缩略图保留独立小尺寸缓存。
- 内置播放处理 P3、PQ/HLG、有限/全范围和旋转信息，输出 sRGB SDR。超高帧率源可跳过非参考帧，持续无法实时解码时明确报告失败。

### 生成与兼容性

- 性能副本生成优先完成，允许 GPU 高负载；兼容输入可在同一 GPU 内解码、缩放和编码，其他输入保留色彩转换及后端回退。
- 转码期间用所选壁纸的高清静态画面减少资源争抢；进度、剩余时间和后端重试有明确状态。
- Windows 最低发布目标统一为 Windows 10 22H2（19045）或 Windows 11 x64；随包部署 Windows App SDK、VC++ Runtime、FFmpeg 和许可证。
- 延续媒体库备份/恢复、事务性移动与删除、外置媒体库身份核对、安装升级与卸载保护。

### 升级与限制

- 安装版沿用原目录覆盖升级，保留配置和媒体库；不要先卸载，卸载会清理安装目录内的默认媒体库与配置。
- 当前未代码签名，SmartScreen 可能提示；HDR 不做 HDR 直通，未完成所有显卡/驱动和干净系统组合的实机验证。
- 两块屏幕可能都显示系统提供的 `Generic PnP Monitor` 名称；条目代表各屏状态，不代表各自运行一个解码器。
- 详细功能、测试证据与边界见 [1.0.0 发布说明](docs/RELEASE_1.0.0.md)。

## [0.1.0] - 2026-09-14

### 调整

- 自动平衡或低功耗副本仍在生成时，用户活动下的桌面播放不再回退到源视频；动态屏保仍保留安全回退。
- 媒体库默认位于安装目录的 `App\Wallpapers`，并支持复制校验后迁移到用户选择的空文件夹；App 与 Agent 会同步热切换到新位置。
- 发布负载 App-local 随附 x64 Visual C++ Runtime，安装版和便携版不再依赖机器预装的全局 VC++ 运行库。
- 安装器明确要求 Windows 10 2004（内部版本 19041）或更高版本，并保留安装目录外的用户媒体库。
- 安装器先对 `Config` / `Wallpapers` 做统一预检，再在非权威 staging 中复制旧数据；它拒绝 junction/reparse point，以路径、类型、64 位大小和 SHA-256 双向复验，并用事务 token 证明可重试目录确属本次迁移后才允许归档。旧源和恢复副本始终保留，双数据根歧义则进入 App/Agent 均不读写的冲突状态。
- 主程序、Agent、Renderer 与安装器写入和校验一致的文件版本、产品版本元数据。
- `balanced` / `power-saver` 与内部 `cpu-smooth` 都升级为 v5 缓存策略，并继续用独立前缀隔离用户画质副本与软件播放兼容副本。
- Visual Studio 18.9 发布构建固定使用不复用的单 MSBuild 节点，避免解决方案 Restore 在高并行度下扩张并残留工作进程。
- 内部 `cpu-smooth` 的 CPU 编码兜底统一为 8-bit SDR OpenH264/H.264：它可以为软件播放流畅度与兼容性转换普通 SDR 10-bit 素材，但始终另写副本而不改写源文件；HDR、BT.2020 或带 HDR 色彩标记的高位深素材不生成该副本。
- 发布负载随附并校验 OpenH264 的 BSD 许可证文本，同时明确第三方 FFmpeg 构建不自动继承 Cisco 官方预编译二进制的专利许可承诺。

### 修复

- 性能副本裁剪在删除每个候选前重新核对活动、退休和 Renderer 生命周期租约，并在最终删除点再次钉住另一份可播放文件；正式副本采用原子替换发布，避免并发切换时误删正在交接或最后一份可播放缓存。
- 首次进入冻结或暂停也会保留低内存压缩面；DComp 压缩瞬时失败后以保守周期重试。
- `auto` 解码不再把“存在 D3D11 视频设备”误当成当前素材可硬解；Agent 会按 H.264、HEVC、VP9、AV1 的 profile、NV12/P010 输出格式和分辨率核对驱动解码配置，缺失时使用隔离的 `cpu-smooth` 路径。
- 混合显卡设备会把通过素材解码探测的适配器 LUID 传给 Renderer，避免在独显探测成功后误用不支持该格式的核显；某一显示路由无法硬解时仅该路由使用 CPU 友好副本，不再连带降级其他可硬解的显示器。等待性能副本时继续发布旧的已确认媒体标识，直到新路由首帧确认。
- 自动平衡与低功耗只在 8-bit SDR 被编码 profile 正向确认时输出 H.264；Unknown、VP9/AV1 高位深或缺失 profile 的素材不再被静默压成 8-bit。
- 上述画质副本规则与内部 `cpu-smooth` 分开：后者可以把普通 SDR 10-bit 转换为 8-bit SDR H.264 兼容副本，但不处理 HDR、BT.2020 或带 HDR 色彩标记的高位深素材，也绝不改写源文件。
- 每次性能副本请求都带独立 token；FFmpeg 进度、成功、失败和清理均在锁定文件句柄后精确比对 `mode|token`，旧任务不能在同档位取消后重试的 ABA 窗口里覆盖新请求或复活“生成中”状态。
- Agent 会先解析实际性能副本，再按该文件的编码、profile、输出格式、尺寸和目标适配器探测；自动解码只有在 Renderer 首帧成功呈现后才确认，启动失败或超时会安全切换到 CPU 友好副本。
- 正在播放的性能副本持有贯穿 Renderer 进程生命周期的租约；已退出的旧 Renderer 会及时释放租约，控制管道断开不再递归清理导致 Agent 栈溢出，且只有确认子进程停止后才会允许媒体库删除或迁移继续。
- 手动删除源文件、副本或整项媒体会先等待 Agent 完全停止 Renderer 与转码线程，不再依赖固定延时；多档副本作为一个带回滚的删除事务处理，失败时不会留下“文件仍在但永久抑制”的半完成状态。
- 外置媒体库设置同时持久化路径与所有权 ID；运行时再由已打开的根目录派生不持久化的 Volume GUID 稳定路径。媒库迁移、副本发布、清理、跨分组移动和删除的实际写操作均使用稳定路径并复核句柄身份；拔盘或盘符被另一块盘复用时保持配置路径用于恢复，但不会在替换盘上继续操作。回收站删除和媒体移动使用随机暂存名及持久恢复记录，中断后可恢复或保留冲突副本，不覆盖用户数据。
- 媒体库迁移会持久化事务 token、源库文件系统身份、阶段和 SHA-256 清单；复制中、准备完成或部分提交后崩溃时，只有完整验证归属且全树无重解析点的遗留目标才会通过已验证目录句柄原子改名为冲突备份并重新开始，无法证明归属的目录保持原样且拒绝覆盖。
- HEVC 画质副本的候选与最终发布都要求 Main10 profile；源文件的 transfer function 或 color primaries 已知时，输出必须精确匹配，否则删除候选并保留原片。
- 极端显示尺寸或聚合像素数的 GPU 负载计算采用饱和乘法，避免整数回绕把重负载错误分配到低性能适配器。
- 本地发布不再信任仅以哈希命名的 FFmpeg 解压目录；每次都复核固定压缩包的 SHA-256，并从全新临时目录解压后再复制和执行。

### 新增

- 性能副本任务显示基于 FFmpeg 已处理媒体时间计算的真实百分比；后端重试会重新计量，最终校验通过前最多显示 99%。

## [0.1.0-alpha.4] - 2026-08-26

### 调整

- 安装版与便携版统一采用单目录数据模式，配置、日志、分组和壁纸均保存在程序目录的 `App` 下。
- 安装新版时自动迁移 `%LOCALAPPDATA%\MotionWallpaper` 中的旧数据；仅在复制全部成功后删除原目录。
- 卸载软件时同步删除配置、日志、分组、壁纸以及遗留的 LocalAppData 数据。

### 验证

- 安装器冒烟测试覆盖单目录标记、运行时数据创建和完整卸载清理。

## [0.1.0-alpha.3] - 2026-08-26

### 调整

- 按设备实际 D3D11 视频能力选择解码与合成路径，高性能设备继续使用物理 GPU，旧显卡优先采用 CPU 解码与物理 GPU 合成。
- 无可用 GPU 时自动生成 CPU 友好的 H.264 性能副本，并按处理器能力选择 1080p60、720p60、720p30 或 480p30。
- 软件渲染根据实际帧耗时在 60、30 和 24 FPS 间动态调节，降低持续满载和卡顿风险。

### 修复

- 修复 NVIDIA 硬件解码被误判成软件解码的问题。
- 修复高分辨率、高帧率视频落入 WARP 后内存接近 800 MB、CPU 持续高占用的问题。
- 增加旧显卡无 D3D11 视频设备时的物理 GPU 合成中间回退，避免过早使用 WARP。

### 验证

- Release x64 构建为零警告、零错误，全部原生测试通过。
- 实机验证 NVIDIA 4K 240 FPS 硬解路径与 WARP CPU 友好副本路径。

## [0.1.0-alpha.2] - 2026-08-25

### 新增

- 增加中英文单 EXE 安装器，可选择安装位置并创建开始菜单或可选桌面快捷方式。
- 使用新的 MotionWallpaper 图标统一应用窗口、任务栏、EXE、快捷方式和安装器外观。
- 常驻 Agent 的系统托盘图标使用同一套多尺寸 Logo，不再显示 Windows 默认应用图标。

### 调整

- 安装版将媒体库、设置和日志保存到 `%LOCALAPPDATA%\MotionWallpaper`，程序目录只保留运行文件。
- 便携 ZIP 使用 `portable.mode` 标记，继续把数据保存在程序目录，兼容已有便携媒体库。
- 发布负载只保留简体中文和英文 WinUI 资源，移除其他未使用的卫星语言目录。
- 卸载程序默认保留用户壁纸和设置，重新安装不会丢失媒体库。

### 验证

- 安装器、便携 ZIP 和对应 SHA-256 校验文件均由发布工作流生成。
- 新增安装版与便携版数据目录选择的原生回归测试。

### 修复

- 修复安装版错误地从用户数据目录寻找 FFmpeg，导致性能副本提示“优化后端未安装”的问题。

## [0.1.0-alpha.1] - 2026-08-25

首个公开 Alpha 测试版本。

### 新增

- 支持仅删除源视频，同时保留性能副本、封面、名称和媒体库元数据。
- 自动平衡与低功耗档位共享同一文件时，按物理文件统计磁盘占用。
- 支持硬件感知的性能副本生成，并提供受负载限制的软件回退方案。
- 增加贡献、安全、许可证、CI 和发布说明。

### 调整

- 删除源文件后，原画播放变为不可用，已有自动平衡或低功耗副本仍可选择。
- 源文件不存在时，壁纸列表继续显示保留的封面和名称。
- 选择壁纸时原地更新卡片状态，不再重建整个媒体列表。
- 启动 Renderer 时优先复用现有 WorkerW 桌面宿主。

### 修复

- 修复连续切换壁纸时短暂白屏和界面卡顿的问题。
- 修复低功耗副本缺少合理大小上限的问题。
- 修复多个性能档位共享存储时重复计算磁盘占用的问题。

### 已知限制

- 当前为未签名的便携预发布包，Windows SmartScreen 可能显示警告。
- 稳定版发布前仍需完成干净环境、升级、卸载、多显示器生命周期、睡眠、锁定/解锁、关闭显示器和 Explorer 重启验证。
