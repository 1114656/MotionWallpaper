# 0.2.0 本地交付记录

日期：2026-09-23。此版本供用户实机验收，尚未公开发布，未执行 Git 推送或创建标签。

## 交付物

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| MotionWallpaper-正式版-v0.2.0-Windows-x64-安装包.exe | 66261610 | `815ed57a68df83b1e524910eba703876b91424e1a6fecab5a9d9153d902217b0` |
| MotionWallpaper-正式版-v0.2.0-Windows-x64-便携版.zip | 94067797 | `68d6e1081c04ecf3e0a2dc8dd781f8e94c6c1c4eaba6cdfb38f3ff905bc4d0a4` |

均位于仓库 `artifacts`，附独立 `.sha256` 文件；打包结束后再次读取最终分发物验证哈希一致。产品版本为 0.2.0，Windows 文件版本为 0.2.0.65535。

## 已完成

- 全量构建、发布目录整理：零警告、零错误，118 项原生测试通过。
- 同一段真实 HEVC Main10 P3 森林视频：转码函数计时由 45.421 秒降至 28.708 秒，输出为 4K60；完整解码和 Windows 首帧通过，原文件哈希不变。具体条件与测量边界见 [提速验证](TRANSCODE_COMPLETION_PRIORITY_2026-09-23.md)。
- GPU 缩放路线及强制拒绝首个预检后的兼容回退，均输出可播放的 1080p60。
- SDR、PQ、HLG、旋转 10-bit MOV 四组色彩与完整解码回归通过。
- 安装器与便携包内容、版本资源、内置依赖、语言资源、许可证与校验和验证通过。
- 隔离安装器冒烟测试通过：媒体组件缺失提示、不可写目录拒绝、改目录升级阻止、同名进程和启动项隔离、中英文资源、App-local VC++ Runtime、单目录数据、迁移中断恢复、事务所有权保护、双标记重试、junction 拒绝、外置媒体保留与卸载清理。
- `git diff --check` 通过。

测试在独立目录完成，没有替换用户现有安装或修改其原视频。更新测试时使用原安装目录覆盖安装；不要先卸载，卸载会清除安装目录内的默认媒体库和配置。旧性能副本仍可复用，观察生成提速需要新生成任务。

## 尚未完成的发布门槛

安装包未代码签名，Windows SmartScreen 可能提示。没有完成干净 Windows 10/11、Intel/AMD、混合显卡及多代驱动矩阵、所有睡眠和多屏热插拔场景、长期运行和外部用户验收；本机自动化通过不等于这些环境已认证。正式面向公众发布前继续按 [发布清单](RELEASING.md) 完成外部验证。
